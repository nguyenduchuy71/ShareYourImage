function CustomScreen({ children }) {
    return (
        <div className="w-full mx-auto my-6 p-6 rounded-2xl border-2 border-transparent lg:border-[#ABF600] lg:w-[50%] md:w-[80%] sm:w-[90%] xs:w-[90%] divide-y-2 divide-[#ABF600]">{children}</div>
    )
}

export default CustomScreen